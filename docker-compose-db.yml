version: '2'
services:
    mysql:
        build: ./src/main/docker/mysql
        container_name: "mysql-sprzecior"
#        volumes:
#          - ./src/main/docker/custom.cnf:/etc/mysql/conf.d/custom.cnf
#            - ./ci/.data/db:/var/lib/mysql
        environment:
            - MYSQL_DATABASE=docker
            - MYSQL_ROOT_PASSWORD=root
        ports:
          - 3307:3306